import API from '../../api'

// initial state
const state = {}

// getters
const getters = {}

// actions
const actions = {
  add ({ commit }) {
  },
  delete ({ commit }) {
  },
  update ({ commit }) {
  },
  get ({ commit }) {
  }
}

// mutations
const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
