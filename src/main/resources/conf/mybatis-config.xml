<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration 
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
        <!-- 命名空间 -->
    <typeAliases>
    	<!-- 这里要写所有要入库的bean -->
        <typeAlias alias="User" type="com.gs.reusebook.bean.User"/> 
    </typeAliases>

    <!-- 映射map -->
    <mappers>
    </mappers>
</configuration>